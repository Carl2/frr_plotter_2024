[tool.pytest]
pythonpath = "."


[tool.poetry]
name = "frr_plotter_2024"
version = "0.0.1"
description = "Parsing zwift frr values"
authors = ["Your Name <olsen.carl@gmail.com>"]
package-mode = false
packages = []  # Explicitly specify no packages to include

[tool.poetry.dependencies]
python = "^3.12"
streamlit = "^1.42.0"
matplotlib = "^3.9.2"
icecream = "^2.1.3"
# Add your other dependencies here

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.build]
generate-setup-file = false
script = "none"